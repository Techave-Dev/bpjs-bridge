// import AES from "crypto-js/aes";
// import Utf8 from "crypto-js/enc-utf8";

// export const decryptBpjs = (data: string, key: string) => {
//   try {
//     const decrypted = AES.decrypt(data, key).toString(Utf8);
//     return JSON.parse(decrypted);
//   } catch (err) {
//     return null;
//   }
// };
